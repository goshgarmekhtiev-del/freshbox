# 📧 Настройка SMTP (Яндекс) для отправки заявок на email

## 📋 Обзор

Endpoint `/api/send-lead.ts` поддерживает отправку заявок не только в Telegram, но и на email через SMTP (Яндекс).

**Важно:** Email отправка является **опциональной** и работает параллельно с Telegram. Ошибки email **не ломают** основной процесс отправки в Telegram.

---

## 🔧 Настройка переменных окружения

### Требуемые переменные

```env
SMTP_HOST=smtp.yandex.ru
SMTP_PORT=465
SMTP_USER=your_yandex_email@yandex.ru
SMTP_PASS=your_yandex_app_password_here
EMAIL_RECEIVER=recipient@example.com
```

### Описание переменных

| Переменная | Описание | Пример |
|------------|----------|--------|
| `SMTP_HOST` | SMTP-сервер Яндекс | `smtp.yandex.ru` |
| `SMTP_PORT` | Порт для SSL-соединения | `465` |
| `SMTP_USER` | Email-адрес Яндекс для отправки | `freshbox@yandex.ru` |
| `SMTP_PASS` | **Пароль приложения** (не обычный пароль!) | `abcdefghijklmnop` |
| `EMAIL_RECEIVER` | Email-адрес получателя заявок | `orders@freshbox.ru` |

---

## 🔑 Получение пароля приложения Яндекс

**ВАЖНО:** `SMTP_PASS` должен быть **паролем приложения**, а не обычным паролем от почты.

### Инструкция:

1. Перейдите на https://id.yandex.ru/security
2. Войдите в свой аккаунт Яндекс
3. Найдите раздел **"Пароли приложений"**
4. Нажмите **"Создать новый пароль"**
5. Укажите название (например, "FreshBox SMTP")
6. Скопируйте сгенерированный пароль
7. Используйте этот пароль в переменной `SMTP_PASS`

**Пример пароля приложения:** `abcdefghijklmnop` (16 символов)

---

## 📝 Настройка для локальной разработки

Создайте файл `.env.local` в корне проекта:

```env
# Telegram (обязательно)
TG_BOT_TOKEN=your_telegram_bot_token
TG_CHAT_ID=your_telegram_chat_id

# SMTP (опционально)
SMTP_HOST=smtp.yandex.ru
SMTP_PORT=465
SMTP_USER=freshbox@yandex.ru
SMTP_PASS=your_yandex_app_password_here
EMAIL_RECEIVER=orders@freshbox.ru
```

---

## 🚀 Настройка для Vercel (Production)

1. Перейдите в **Vercel Dashboard**
2. Откройте проект → **Settings** → **Environment Variables**
3. Добавьте переменные:

   ```
   SMTP_HOST = smtp.yandex.ru
   SMTP_PORT = 465
   SMTP_USER = freshbox@yandex.ru
   SMTP_PASS = your_yandex_app_password_here
   EMAIL_RECEIVER = orders@freshbox.ru
   ```

4. Выберите окружения: **Production**, **Preview**, **Development**
5. Сохраните изменения
6. Передеплойте проект (если нужно)

---

## 📧 Формат email-письма

### Тема письма

- **Для заказов:** `Новый заказ с сайта FreshBox`
- **Для B2B-заявок:** `Новая B2B-заявка FreshBox`

### Содержимое

Email отправляется в формате **HTML** с красивым оформлением:

- Заголовок с эмодзи 🍊
- Структурированные поля (Тип, Имя, Телефон, Email, Комментарий)
- Для заказов: список товаров из корзины
- Для заказов: адрес доставки и время
- Футер с временем получения заявки

### Пример для B2B:

```
🍊 FreshBox — новая заявка!

Тип: B2B
Имя: Иван Иванов
Телефон: +7 (999) 000-00-00
Email: ivan@example.com
Комментарий: Нужен корпоративный заказ
```

### Пример для заказа:

```
🍊 FreshBox — новая заявка!

Тип: Order
Имя: Мария Петрова
Телефон: +7 (999) 111-22-33
Email: maria@example.com

Корзина:
• Средний бокс • Классика — 2 шт (4200 ₽)
• Большой бокс • Тропики — 1 шт (6500 ₽)
Адрес доставки: Москва, ул. Примерная, д. 1
Время доставки: Сегодня, 14:00
```

---

## 🔄 Логика работы

### Последовательность отправки:

1. **Валидация данных** заявки
2. **Отправка в Telegram** (основной канал)
   - Если Telegram не отправился → возвращается ошибка
3. **Отправка на email** (если настроен SMTP)
   - Если email не отправился → ошибка логируется, но процесс продолжается
   - Telegram уже отправлен, поэтому возвращается успех

### Обработка ошибок:

- **Ошибка Telegram:** возвращается `500` с описанием ошибки
- **Ошибка Email:** логируется в консоль, но не прерывает процесс
- **Отсутствие SMTP переменных:** email просто не отправляется, Telegram работает

---

## ✅ Проверка работы

### 1. Проверка переменных окружения

Убедитесь, что все переменные настроены:

```bash
# Локально (в .env.local)
cat .env.local | grep SMTP

# На Vercel (через Dashboard)
# Settings → Environment Variables
```

### 2. Тестовая отправка

Отправьте тестовую заявку через форму на сайте или через curl:

```bash
curl -X POST http://localhost:3000/api/send-lead \
  -H "Content-Type: application/json" \
  -d '{
    "type": "B2B",
    "name": "Test User",
    "phone": "+79990000000",
    "email": "test@example.com",
    "comment": "Test email sending"
  }'
```

### 3. Проверка логов

- **Локально:** смотрите консоль терминала
- **На Vercel:** смотрите логи в Vercel Dashboard → Functions → Logs

**Успешная отправка email:**
```
Email sent successfully
```

**Ошибка отправки email:**
```
Email send error: [описание ошибки]
```

### 4. Проверка почты

Проверьте почтовый ящик, указанный в `EMAIL_RECEIVER`. Письмо должно прийти в течение нескольких секунд.

---

## 🐛 Решение проблем

### Проблема: Email не отправляется

**Возможные причины:**

1. **Неверный пароль приложения**
   - Убедитесь, что используете пароль приложения, а не обычный пароль
   - Создайте новый пароль приложения на https://id.yandex.ru/security

2. **Неверные SMTP настройки**
   - Проверьте `SMTP_HOST` = `smtp.yandex.ru`
   - Проверьте `SMTP_PORT` = `465`
   - Проверьте `SMTP_USER` (должен быть полный email)

3. **Переменные окружения не настроены**
   - Проверьте, что все переменные добавлены в `.env.local` (локально) или Vercel Dashboard (production)

4. **Ошибка в логах**
   - Проверьте логи в консоли (локально) или Vercel Dashboard (production)
   - Ищите строки с `Email send error:`

### Проблема: "Authentication failed"

**Решение:**
- Убедитесь, что используете **пароль приложения**, а не обычный пароль
- Проверьте, что пароль приложения создан для правильного аккаунта

### Проблема: "Connection timeout"

**Решение:**
- Проверьте, что `SMTP_HOST` = `smtp.yandex.ru`
- Проверьте, что `SMTP_PORT` = `465`
- Убедитесь, что используется `secure: true` (SSL)

---

## 📚 Дополнительная информация

- **Nodemailer документация:** https://nodemailer.com/
- **Яндекс SMTP настройки:** https://yandex.ru/support/mail/mail-clients.html
- **Пароли приложений Яндекс:** https://yandex.ru/support/id/authorization/app-passwords.html

---

## ✅ Итог

После настройки SMTP переменных:

- ✅ Заявки отправляются в Telegram (как раньше)
- ✅ Заявки **также** отправляются на email
- ✅ Ошибки email не ломают Telegram-отправку
- ✅ Email письма имеют красивое HTML-оформление
- ✅ Все данные заявки корректно отображаются в письме

**Готово к использованию!** 🚀

